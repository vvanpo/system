#!/usr/bin/env python3
import grammar

g = grammar.grammar("""
S = production S
S = production
  production = A "=" A newline
    A = string
      string = non-terminal string
      string = terminal string
      string = non-terminal
      string = terminal
    A = empty-string
      empty-string = 03b5
    newline = 0a
""")

